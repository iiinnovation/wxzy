# Copy to keystore.properties. The real file and keystore are ignored by Git.
# storeFile may be absolute or relative to mobile/android.
storeFile=/absolute/path/to/wenxi-release.keystore
storePassword=replace-with-owner-managed-password
keyAlias=wenxi
keyPassword=replace-with-owner-managed-password
